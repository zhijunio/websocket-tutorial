# websocket-tutorial：WebSocket 学习课程大纲

这份大纲把 `MISSION.md` 的能力目标拆成可执行的课程和验收门槛。课程主线使用 Java 25、Spring Boot 4.1.1、Maven 和 Docker；协议依据优先使用 RFC 6455、官方文档、DevTools、抓包和日志。

## 学习方式

- 每课只解决一个核心能力，并包含回忆题、最小示例、动手练习、验证方式和故障案例。
- 代码型课程必须讲解关键源码路径、状态不变量和测试证明范围；不做逐行翻译。
- 对协议时序、状态模型和架构边界，适当使用可复用图片或图示帮助理解。
- 理论或抓包课只生成课程材料；代码型课程在 `examples/` 下拥有一个独立、可运行的项目。
- 课程示例不共享构建目录和隐式代码。每个示例都应能在自己的目录中执行 `mvn test` 和 `mvn spring-boot:run`。
- 未完成当前课程的练习和验证，不自动进入下一阶段。
- 课程会区分协议标准、Spring 行为、Jakarta 容器行为、代理行为和生产实践。
- 当前基线为 Java 25、Spring Boot 4.1.1、Maven 和 Docker，并与 `MISSION.md` 保持一致。

## 阶段总览

| 阶段 | 目标 | 课程 | 主要产出 |
|---|---|---|---|
| A. 协议基础 | 能读握手、帧和连接生命周期 | 0001-0002 | DevTools/抓包分析、协议速查 |
| B. 服务端模型 | 能用两种 Java 服务端模型跑通双向消息 | 0003-0004 | 两个独立回显项目 |
| C. 业务能力 | 能实现多房间聊天和可靠通知 | 0005-0007 | G1、G2 项目 |
| D. 生产边界 | 能处理心跳、鉴权、代理、多实例和消息子协议 | 0008-0009 | 故障实验、扩展架构图、STOMP 示例和选型说明 |
| E. 系统集成 | 能解释 MQTT 桥接并完成性能验证 | 0010-0011 | MQTT 桥接设计、G3 基准报告 |
| F. 结业验收 | 能进行架构表达、故障排查和技术选型 | 0012 | 三个项目的 README、面试和白板验收 |

## 课程清单

### A. 协议基础

| 课程 | 核心能力 | 产出 | 状态 |
|---|---|---|---|
| 0001 Opening Handshake 与 DevTools | 了解轮询、长轮询到 WebSocket 的发展背景；识别 `101`、`Upgrade`、`Sec-WebSocket-*`，区分握手和认证 | 握手分析与 5 个回忆题 | 已生成 |
| 0002 WebSocket 帧与连接生命周期 | 区分消息、帧、控制帧、掩码和 Close 流程 | 帧头解读、生命周期分析 | 已生成 |

### B. 服务端模型

| 课程 | 核心能力 | 示例项目 | 状态 |
|---|---|---|---|
| 0003 Spring Boot 双向消息 Demo | 使用 `TextWebSocketHandler` 完成回显，并在 DevTools 验证 101 | `examples/0003-spring-boot-websocket-echo` | 已生成，Maven 测试通过 |
| 0004 Jakarta `@ServerEndpoint` 对照 | 使用 `ServerEndpointExporter` 完成标准 API 实现，比较两种模型 | `examples/0004-jakarta-server-endpoint` | 已生成，Maven 测试通过 |

### C. 业务能力

| 课程 | 核心能力 | 验收 |
|---|---|---|
| 0005 多房间协议与状态 | 设计加入、离开、广播和错误消息；保证不同房间不串消息 | G1 初版可运行，Maven 测试通过 |
| 0006 心跳、断线和安全边界 | 处理 Ping/Pong、业务心跳、Origin、鉴权、重连和断开清理 | Maven 测试通过，待浏览器故障演练 |
| 0007 推送通知与补拉 | 处理在线推送、离线未读标记或补拉，并明确消息幂等性 | G2 初版可运行，Maven 测试通过 |

### D. 生产边界

| 课程 | 核心能力 | 验收 |
|---|---|---|
| 0008 代理、网关和多实例 | 排查 idle timeout，设计连接归属、消息路由和水平扩展 | Maven 测试通过，待 Compose/DevTools 演练 |
| 0009 STOMP over WebSocket | 实现 `CONNECT`、`SUBSCRIBE`、`SEND`、`MESSAGE`，理解 `ACK`、Spring Message Broker、目的地路由、心跳和 broker relay | 独立 `examples/0009-stomp-websocket` 项目，Maven 测试通过；ACK 当前为协议讲解，示例使用 `ack:auto` |

### E. 系统集成与性能

| 课程 | 核心能力 | 验收 |
|---|---|---|
| 0010 MQTT 桥接 | 实现 MQTT Broker ↔ Java 后端 ↔ WebSocket 客户端桥接，说明 Spring Integration MQTT / Paho 角色 | 独立示例项目，Maven 测试通过，桥接图已生成 |
| 0011 高频小消息基准 | 建立 100 客户端、1000 msg/s、约 256 字节、60 秒的可重复测试 | 工具统计边界已修正，G3 正式三次基准已完成；尚未做服务端优化前后对照 |

### F. 结业验收

| 课程 | 核心能力 | 验收 |
|---|---|---|
| 0012 故障排查与面试演练 | 用“问题—方案—权衡”表达 G1/G2/G3，白板画握手和扩展架构 | 课程已生成，需完成 G1/G2/G3 证据和面试演练 |

Netty 作为进阶插入点：在 0008 或 0012 中说明管线职责、收益和代价；不进入源码级学习，除非性能实验暴露出明确的技术问题。

## 必须补齐的知识点

这些内容已经有对应的课程位置，但需要在正式授课时明确做成练习或验收项，不能只作为名词出现：

| 知识点 | 放入课程 | 必须形成的能力 |
|---|---|---|
| 轮询、长轮询、Keep-Alive 与 WebSocket | 0001 | 用连接数、请求频率、延迟和服务端资源解释差异，而不是只说“WebSocket 更实时” |
| 子协议与扩展 | 0002、0009 | 识别 `Sec-WebSocket-Protocol`、`Sec-WebSocket-Extensions`，说明 STOMP、SockJS 和 `permessage-deflate` 的协商位置与代价 |
| Close code 与错误语义 | 0002、0006 | 区分正常关闭、协议错误、策略拒绝、服务端异常，并据此决定是否重连 |
| 浏览器客户端状态与重连 | 0006 | 使用 `readyState`、`bufferedAmount`、退避、抖动和重连上限，避免重连风暴 |
| 并发安全与背压 | 0005、0008、0011 | 处理广播并发、慢客户端、有界队列、发送顺序、消息大小和连接数限制 |
| 可靠性语义 | 0007、0008 | 说明顺序、重复、丢失、幂等、ack、序列号、补拉和断线恢复的取舍 |
| 安全模型 | 0006 | 区分 Origin 校验、CSRF、Cookie/Token 鉴权、授权、TLS、输入校验和限流 |
| 可观测性与测试 | 0006、0008、0011 | 记录连接数、活跃会话、close code、队列深度、延迟和错误；覆盖握手、断线、慢客户端和并发广播 |
| MQTT 实际语义 | 0010 | 说明 topic 映射、QoS、retain、session expiry、桥接失败和背压，不把 MQTT 只当作“另一个 WebSocket” |

## 阶段门槛

### G1 多房间聊天

- 支持多个房间、加入/离开和房间内广播。
- 不同房间不串消息。
- 断开后成员状态最终清理。
- 有消息协议、架构图、测试、启动命令和至少一个失败案例。

### G2 推送通知

- 支持在线推送。
- 断线期间有未读标记或补拉机制。
- 能说明单实例和多实例下消息如何到达目标连接。
- 有幂等、鉴权、日志和故障排查说明。

### G3 高频小消息

- 100 个客户端。
- 每客户端每秒 10 条、每条约 256 字节。
- 持续 60 秒，重复 3 次。
- 记录吞吐、p50/p95 延迟、错误率、CPU 和内存。
- 解释优化前后的瓶颈和权衡；`p95 < 200ms` 只是学习基线，不是生产 SLO。

## 每课完成条件

1. 能不用复制粘贴解释本课的核心模型。
2. 能运行示例或完成抓包/故障实验。
3. 能回答课内回忆题，并修正错误理解。
4. 能指出一个现实工程风险和对应验证方式。
5. 代码型课程的示例项目通过 `mvn test`，并在 README 中写明启动和验证命令。
